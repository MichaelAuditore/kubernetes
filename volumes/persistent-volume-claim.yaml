apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-storage
  labels:
    app: blue-app
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
